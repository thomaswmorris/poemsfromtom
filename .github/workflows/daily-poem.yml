name: Send the daily poem
on:
  schedule:
    - cron: '0 12 * * *' # hourly # daily at 12:00 UTC (07:00 EST)

jobs:
  run_tests:
    name: Send a daily poem at noon UTC
    runs-on: ubuntu-latest

    defaults:
      run:
        shell: bash -l {0}

    steps:
      - name: Checkout the code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Curate a poem, and send it to the listserv
        run:
          python poetry/send-poem.py --type 'daily' --username 'poemsfromtom@gmail.com' --password ${{ secrets.PFT_PW }} --listserv_filename 'poems/listserv.csv'
          --repo 'thomaswmorris.github.io' --token ${{ secrets.GH_TOKEN }} --context true --rh true --wh true --vv true

      - name: Update repo pages
        run:
          python poetry/make-docs.py --repo 'thomaswmorris.github.io' --token ${{ secrets.GH_TOKEN }} 